"""LLM service for clinical note generation."""

async def generate_notes(transcript: str, language: str = "sw") -> dict:
    """Generate clinical notes from transcript."""
    raise NotImplementedError
